spring:
  profiles:
    active: dev
  application:
    name: marketNews
  cloud:
    config:
      uri: http://localhost:8000
server:
  port: 8002
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka
      