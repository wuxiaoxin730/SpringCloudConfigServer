spring:
  profiles:
    active: dev
  application:
    name: eurekaService
  cloud:
    config:
      uri: http://localhost:8000
server:
  port: 8001
  
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false